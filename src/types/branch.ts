export type RawBranch = {
    branch_name: string
    branch_code: string
    address: string
    post_code: string
    sort_order: string
}

export type Branch = {
    branchName: string
    branchCode: string
    postCode: string
    address: string
    sortOrder: string
}