export type RawBranch = {
    swift_code: string
    branch_name: string
    branch_code: string
    address: string
    post_code: string
    sort_order: string
}

export type Branch = {
    swiftCode: string
    branchName: string
    branchCode: string
    postCode: string
    address: string
    sortOrder: string
}